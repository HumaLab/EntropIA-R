# This file is part of the standard setup for testthat.
# If you find yourself running this file when you really need to run the
# tests, consider running `devtools::test()` instead.
library(testthat)
library(entropiaR)

test_check("entropiaR")
