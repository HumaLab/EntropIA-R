% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{entropia_validate}
\alias{entropia_validate}
\title{Validate the structure of an EntropIA database}
\usage{
entropia_validate(con)
}
\arguments{
\item{con}{A connection returned by \code{\link[=entropia_connect]{entropia_connect()}}.}
}
\value{
A tibble of findings with columns \code{severity}, \code{kind}, \code{table},
\code{column} and \code{message}, plus \code{row_counts} and \code{schema_version} attributes.
}
\description{
Reports structural health as a tibble of findings. The check is read-only
and \code{EXPLAIN}-free: it compares the live schema (\code{sqlite_master} +
\verb{PRAGMA table_xinfo}) against the shipped column contract and counts rows.
}
\details{
Each finding has a \code{severity} (\code{"error"}, \code{"warning"}, \code{"info"}), a \code{kind}
(\code{"table_missing"}, \code{"column_missing"}, \code{"empty_database"},
\code{"no_migrations"}, or \code{"unreadable"}), the affected \code{table}/\code{column} (or
\code{NA}), and a plain-text \code{message}. A healthy database returns an empty
findings tibble: zero error-severity findings and no warnings for a
complete post-0029 schema. Row counts for every readable table and the
detected schema version are attached as the \code{row_counts} and
\code{schema_version} attributes.

Unlike the accessors, \code{validate()} never raises \code{entropia_error_table_missing}
or \code{entropia_error_column_missing}: structural gaps are findings, not errors.
The one exception is an unreadable (corrupt) file, which yields a single
\code{"unreadable"} finding of severity \code{"error"}.
}
\examples{
con <- entropia_connect(system.file("extdata", "entropia-example.sqlite",
  package = "entropiaR"))
entropia_validate(con) # zero findings on the example database
entropia_disconnect(con)
}
