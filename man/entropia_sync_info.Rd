% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sync.R
\name{entropia_sync_info}
\alias{entropia_sync_info}
\title{Whitelisted sync settings as a typed tibble}
\usage{
entropia_sync_info(con)
}
\arguments{
\item{con}{A connection returned by \code{\link[=entropia_connect]{entropia_connect()}}.}
}
\value{
A one-row tibble with columns \code{device_id}, \code{account_email},
\code{server_url}, \code{last_sync_at}, \code{server_epoch}, \code{triggers_version} and
\code{capture_enabled}. Keys absent from the database are \code{NA} of the correct
type.
}
\description{
Reads the sync engine's \code{sync_meta} key/value table and returns only the
whitelisted keys -- the ones that describe sync identity and freshness --
coerced to their natural R types. \code{last_sync_at} (epoch milliseconds) is
returned as \code{POSIXct}; \code{server_epoch} stays character (the app stores a
server/session identifier string, not a numeric epoch); \code{triggers_version}
as an integer; \code{capture_enabled} as a logical.
}
\details{
\code{app_settings} is never part of this surface: it stores API secrets
(\verb{*_api_key}) and is deliberately not surfaced raw.
}
\examples{
con <- entropia_connect(system.file("extdata", "entropia-example.sqlite",
  package = "entropiaR"))
entropia_sync_info(con)
entropia_disconnect(con)
}
