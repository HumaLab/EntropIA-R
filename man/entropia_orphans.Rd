% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quality.R
\name{entropia_orphans}
\alias{entropia_orphans}
\title{Detect orphaned rows (broken conceptual foreign keys)}
\usage{
entropia_orphans(con)
}
\arguments{
\item{con}{A connection returned by \code{\link[=entropia_connect]{entropia_connect()}}.}
}
\value{
A tibble of class \code{entropia_orphans} with columns \code{kind}, \code{table},
\code{id}, \code{column}, \code{ref_table} and \code{message}, ordered by \code{kind} then \code{id}.
}
\description{
EntropIA declares most relationships only conceptually (many have no
physical foreign-key constraint), so a row can silently point at a parent
that does not exist. \code{entropia_orphans()} scans every conceptual foreign key
in the schema and reports one row per broken reference:
}
\details{
\itemize{
\item \code{items.collection_id} -> \code{collections.id}
\item \code{assets.item_id} -> \code{items.id} and \code{assets.parent_asset_id} -> \code{assets.id}
\item \code{extractions}/\code{transcriptions}/\code{layouts}.\code{asset_id} -> \code{assets.id}
\item \code{entities.item_id} -> \code{items.id} (and \code{asset_id} -> \code{assets.id} when set)
\item \code{triples.item_id} -> \code{items.id} (and \code{asset_id} -> \code{assets.id} when set)
\item \code{notes.item_id} -> \code{items.id} (and \code{asset_id} -> \code{assets.id} when set)
\item \code{annotations.asset_id} -> \code{assets.id}
\item \code{llm_results.target_id} -> the table named by \code{target_type}
(\code{asset}/\code{item}/\code{collection}; \code{unknown} and NULL targets are not checkable)
\item \code{rag_messages.conversation_id} -> \code{rag_conversations.id}
\item \code{rag_chunks.item_id}/\code{asset_id} -> \code{items.id}/\code{assets.id}
\item \code{item_topics.item_id}/\code{topic_id} -> \code{items.id}/\code{topics.id}
}

NULL foreign keys are never reported: they are the documented "item-level"
value for the optional \code{asset_id} columns, and the required FKs are \verb{NOT NULL} in the schema. Tables absent from the database are skipped, so minimal
and legacy schemas degrade gracefully. The result is materialised (it is a
small diagnostic, like \code{\link[=entropia_validate]{entropia_validate()}} findings) and carries the
\code{entropia_orphans} class.
}
\examples{
con <- entropia_connect(system.file("extdata", "entropia-example.sqlite",
  package = "entropiaR"))
entropia_orphans(con) # zero findings on the example database
entropia_disconnect(con)
}
