% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tables.R
\name{entropia_triples}
\alias{entropia_triples}
\title{Triples (lazy)}
\usage{
entropia_triples(con)
}
\arguments{
\item{con}{A connection returned by \code{\link[=entropia_connect]{entropia_connect()}}.}
}
\value{
A \code{tbl_sql} on \code{triples}.
}
\description{
A lazy \code{\link[dplyr:tbl]{dplyr::tbl()}} over the \code{triples} table (subject/predicate/object
extractions). \code{created_at} uses the magnitude-guarded \code{datetime_auto}
contract; \code{asset_id} is NULL for item-level triples.
}
\examples{
con <- entropia_connect(system.file("extdata", "entropia-example.sqlite",
  package = "entropiaR"))
entropia_collect(entropia_triples(con))
entropia_disconnect(con)
}
