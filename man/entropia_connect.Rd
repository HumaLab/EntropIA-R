% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connect.R
\name{entropia_connect}
\alias{entropia_connect}
\title{Connect to an EntropIA SQLite database (read-only)}
\usage{
entropia_connect(path, write = FALSE, validate = TRUE, quiet = FALSE)
}
\arguments{
\item{path}{Path to the EntropIA SQLite database, or \code{":memory:"}.}

\item{write}{Must be \code{FALSE} in v1 (read-only). Passing \code{TRUE} errors with
class \code{entropia_error_write_disabled} and v2 guidance.}

\item{validate}{Logical. When \code{TRUE} (default) the connection runs a
lightweight sanity query (rejecting files that are not readable SQLite
databases) and applies the schema compatibility policy controlled by
\code{options(entropiaR.schema_policy)}.}

\item{quiet}{Logical. When \code{TRUE}, suppresses the schema-policy warning
emitted on open (only affects the default \code{"warn"} policy; errors are
never silenced).}
}
\value{
An \code{entropia_conn} object (S4, \code{SQLiteConnection} subclass).
}
\description{
Opens \code{path} as a read-only DBI connection. The returned object is an
\code{entropia_conn}: a typed subclass of \link[RSQLite:SQLiteConnection-class]{RSQLite::SQLiteConnection}
carrying the \code{path}, \code{mode}, \code{schema_version} and \code{content_hash}
attributes. All DBI and dbplyr functions keep working on it.
}
\examples{
con <- entropia_connect(system.file("extdata", "entropia-example.sqlite",
  package = "entropiaR"
))
entropia_status(con)
entropia_disconnect(con)
}
