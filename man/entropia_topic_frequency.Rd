% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{entropia_topic_frequency}
\alias{entropia_topic_frequency}
\title{Topic frequency (items per topic)}
\usage{
entropia_topic_frequency(x, by = NULL)
}
\arguments{
\item{x}{A data frame or tibble with a \code{name} column holding topic names.}

\item{by}{Optional column(s) to break the counts by, selected by name or
bare (tidyselect). \code{NULL} (default) produces one row per topic.}
}
\value{
A tibble with the \code{by} columns (when given), \code{name} and \code{n} (item
count), ordered by \code{n} descending then \code{name}.
}
\description{
Counts rows per topic from a collected tibble carrying a topic \code{name}
column. The natural input is \code{item_topics} joined to \code{topics} (e.g.
\code{dplyr::left_join(entropia_collect(entropia_item_topics(con)), entropia_collect(entropia_topics(con)), by = c("topic_id" = "id"))}).
Because \code{item_topics} has a \code{UNIQUE(item_id, topic_id)} constraint, counting
rows per topic counts items per topic. The result is ordered by descending
count then topic name (deterministic).
}
\examples{
con <- entropia_connect(system.file("extdata", "entropia-example.sqlite",
  package = "entropiaR"))
item_topics <- entropia_collect(entropia_item_topics(con))
topics <- entropia_collect(entropia_topics(con))
joined <- dplyr::left_join(item_topics, topics, by = c("topic_id" = "id"))
entropia_topic_frequency(joined)
entropia_disconnect(con)
}
