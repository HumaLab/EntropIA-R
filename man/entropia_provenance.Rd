% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{entropia_provenance}
\alias{entropia_provenance}
\title{Read the provenance stamp of a reproducible dataset}
\usage{
entropia_provenance(x)
}
\arguments{
\item{x}{An object carrying a provenance stamp, e.g. the output of
\code{\link[=entropia_analysis_dataset]{entropia_analysis_dataset()}}.}
}
\value{
A list of class \code{entropia_provenance}.
}
\description{
Returns the \code{entropia_prov} attribute attached by
\code{\link[=entropia_analysis_dataset]{entropia_analysis_dataset()}}: a list recording the dataset \code{name}, the
database \code{schema_version}, the schema \code{content_hash}, the \code{source_path}, the
captured \code{filters}, the \code{package_version}, the \code{built_at} timestamp and the
\code{r_version}. Aborts with \code{entropia_error_invalid_argument} when \code{x} carries
no provenance stamp.
}
\examples{
con <- entropia_connect(system.file("extdata", "entropia-example.sqlite",
  package = "entropiaR"
))
ds <- entropia_analysis_dataset(con, asset_type == "image")
entropia_provenance(ds)
entropia_disconnect(con)
}
