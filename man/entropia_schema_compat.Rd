% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema.R
\name{entropia_schema_compat}
\alias{entropia_schema_compat}
\title{Schema compatibility status of an EntropIA database}
\usage{
entropia_schema_compat(con)
}
\arguments{
\item{con}{A connection returned by \code{\link[=entropia_connect]{entropia_connect()}}.}
}
\value{
A list of class \code{entropia_schema_compat} with elements \code{status},
\code{version}, \code{manifest_head}, \code{gaps} (the full expected-but-absent column
table), \code{required_missing}, \code{optional_missing} and \code{compatible}.
}
\description{
Classifies a database against the shipped column contract
(\code{inst/schemas/manifest.json}). The status is one of:
}
\details{
\itemize{
\item \code{"known"}: the schema version matches the manifest head (\verb{0029_rag_chunks}).
\item \code{"newer"}: the database version is lexicographically ahead of the manifest
head -- a future EntropIA wrote it.
\item \code{"older"}: the database version predates the manifest head.
\item \code{"unknown"}: no \verb{_migrations} table (or an empty one); version undetectable.
}

The required-column check uses the manifest contract: columns tagged
\code{required = TRUE} whose \code{min_version} is already reached by the database
version must exist in the live schema. Missing ones are listed in
\code{required_missing} and make the database incompatible.
}
\examples{
con <- entropia_connect(system.file("extdata", "entropia-example.sqlite",
  package = "entropiaR"
))
entropia_schema_compat(con)
entropia_disconnect(con)
}
