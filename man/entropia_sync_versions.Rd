% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sync.R
\name{entropia_sync_versions}
\alias{entropia_sync_versions}
\title{Sync row versions (lazy)}
\usage{
entropia_sync_versions(con)
}
\arguments{
\item{con}{A connection returned by \code{\link[=entropia_connect]{entropia_connect()}}.}
}
\value{
A \code{tbl_sql} on \code{sync_row_versions}.
}
\description{
A lazy \code{\link[dplyr:tbl]{dplyr::tbl()}} over \code{sync_row_versions}, which maps every synced row
\verb{(table_name, row_id)} to the server sequence number it was last seen at.
The result is uncollected, so filtering, joining and aggregating happen in
SQLite; call \code{entropia_collect()} (or \code{dplyr::collect()}) to bring rows into
R.
}
