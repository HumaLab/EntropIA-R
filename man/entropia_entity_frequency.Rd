% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{entropia_entity_frequency}
\alias{entropia_entity_frequency}
\title{Entity frequency (top entities by type)}
\usage{
entropia_entity_frequency(x, by = NULL)
}
\arguments{
\item{x}{A data frame or tibble of collected entity rows, carrying
\code{entity_type} and \code{value} columns.}

\item{by}{Optional column(s) to break the counts by, selected by name or
bare (tidyselect). \code{NULL} (default) produces one row per
\code{entity_type} x \code{value}.}
}
\value{
A tibble with the \code{by} columns (when given), \code{entity_type}, \code{value}
and \code{n} (row count), ordered by \code{entity_type} then \code{n} descending then
\code{value}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
Counts entity occurrences from a collected entities tibble: for every
distinct \code{entity_type} x \code{value} pair, the number of rows carrying it. The
result is ordered by \code{entity_type} then descending count, so the top
entities of each type read off the top of each block. Pass the collected
output of \code{\link[=entropia_entities]{entropia_entities()}} directly, or join it to \code{items} and
\code{collections} first and pass \code{by} to break the counts down further (e.g. by
\code{collection_name}). Soft-deleted rows are whatever the input carries -- use
\code{include_deleted = TRUE} on \code{\link[=entropia_entities]{entropia_entities()}} to count them.
}
\examples{
con <- entropia_connect(system.file("extdata", "entropia-example.sqlite",
  package = "entropiaR"
))
entities <- entropia_collect(entropia_entities(con))
entropia_entity_frequency(entities)
entropia_disconnect(con)
}
