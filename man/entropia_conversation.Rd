% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/research.R
\name{entropia_conversation}
\alias{entropia_conversation}
\title{One RAG conversation with its ordered messages}
\usage{
entropia_conversation(con, id)
}
\arguments{
\item{con}{A connection returned by \code{\link[=entropia_connect]{entropia_connect()}}.}

\item{id}{A single conversation id (a value of \code{rag_conversations.id}).}
}
\value{
A list of class \code{entropia_conversation}.
}
\description{
Materialises a single retrieval-augmented chat conversation: the
\code{rag_conversations} row plus its \code{rag_messages}, ordered by \code{sort_index}.
The \code{sources} citations on assistant messages are parsed from JSON into a
list-column (each element a data.frame of \verb{\{chunk_id, text, score\}} rows);
user messages carry \code{NA}. Timestamps become \code{POSIXct}.
}
\details{
Returns a list of class \code{entropia_conversation} with two elements:
\itemize{
\item \code{conversation}: a one-row tibble (id, title, created_at, updated_at).
\item \code{messages}: a tibble with \code{id}, \code{sort_index}, \code{role}, \code{content},
\code{sources}, \code{model} and \code{created_at}, ordered by \code{sort_index}.
}
}
\examples{
con <- entropia_connect(system.file("extdata", "entropia-example.sqlite",
  package = "entropiaR"))
entropia_conversation(con, "bbbbbbbb-bbbb-4bbb-8bbb-bbbbbbbbbbb1")
entropia_disconnect(con)
}
