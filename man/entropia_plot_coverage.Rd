% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{entropia_plot_coverage}
\alias{entropia_plot_coverage}
\title{Plot corpus coverage}
\usage{
entropia_plot_coverage(x, metric = NULL)
}
\arguments{
\item{x}{A data frame or tibble with \code{metric}, \code{group} and \code{pct} columns,
e.g. the output of \code{\link[=entropia_corpus_quality]{entropia_corpus_quality()}}.}

\item{metric}{Optional character vector of metric(s) to plot, a subset of
\code{unique(x$metric)}. \code{NULL} (default) plots all metrics.}
}
\value{
A \code{ggplot} object.
}
\description{
Plots the output of \code{\link[=entropia_corpus_quality]{entropia_corpus_quality()}} -- coverage proportions per
metric and group -- as a bar chart. With a single \code{metric} the chart is a
plain bar chart of the coverage proportion by group; with several metrics the
bars are faceted by \code{metric}, each panel carrying its own groups and y scale.
}
\details{
The y axis is labelled as a percentage. \code{pct} is \code{NA} for groups with no
total (e.g. a metric absent from a corpus); these rows receive an explicit
annotation, using \code{status} when available, instead of a misleading zero bar.
Optional \code{group_id} is displayed alongside the label; distinct IDs never
merge. Empty selections produce a Spanish no-data annotation.
}
\examples{
q <- data.frame(
  metric = c("ocr_coverage", "metadata_coverage"),
  group = c("image", "Archivo de prueba"),
  n = c(250L, 3L),
  total = c(2428L, 3L),
  pct = c(250 / 2428, 1)
)
if (requireNamespace("ggplot2", quietly = TRUE)) {
  p <- entropia_plot_coverage(q)
  p + ggplot2::labs(title = "Cobertura")
}
}
