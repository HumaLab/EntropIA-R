% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/research.R
\name{entropia_entity_relations}
\alias{entropia_entity_relations}
\title{Entity relations (triples with item and collection context)}
\usage{
entropia_entity_relations(con, min_confidence = NULL)
}
\arguments{
\item{con}{A connection returned by \code{\link[=entropia_connect]{entropia_connect()}}.}

\item{min_confidence}{Unused in the current schema (triples carry no
confidence column); must be \code{NULL} (the default).}
}
\value{
A \code{tbl_sql}.
}
\description{
A lazy \code{\link[dplyr:tbl]{dplyr::tbl()}} over \code{triples} joined to the item and collection it
came from, so every subject/predicate/object extraction carries its
provenance:
}
\details{
\itemize{
\item \code{id}, \code{subject}, \code{predicate}, \code{object}: the triple itself.
\item \code{item_id}, \code{item_title}: the owning item and its title.
\item \code{collection_id}, \code{collection_name}: the item's collection.
\item \code{asset_id}: the specific asset the triple is scoped to, \code{NA} for
item-level triples.
}

Everything stays lazy: the joins run in SQLite when the result is collected.

\code{min_confidence} is accepted for API stability with the plan's research
layer, but the current schema's \code{triples} table has no confidence column, so
passing a non-\code{NULL} value errors with guidance rather than silently doing
nothing.
}
