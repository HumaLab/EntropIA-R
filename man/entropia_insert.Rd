% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write.R
\name{entropia_insert}
\alias{entropia_insert}
\title{Insert rows (v2 contract; errors in v1)}
\usage{
entropia_insert(con, table, data, dry_run = TRUE)
}
\arguments{
\item{con}{An EntropIA connection.}

\item{table}{Table name.}

\item{data}{Data to insert (data frame/tibble).}

\item{dry_run}{Logical. In v2, validate without writing when \code{TRUE} (default).}
}
\value{
Never returns: aborts with \code{entropia_error_write_disabled}.
}
\description{
The write API is designed for v2 and shipped in v1 as stubs that error with
class \code{entropia_error_write_disabled}. Every call aborts immediately with
guidance: v1 is read-only, and write support arrives in v2 via
\code{entropia_connect(path, write = TRUE)}.
}
\details{
In v2, \code{entropia_insert()} inserts validated rows in one transaction,
generating UUIDv4 primary keys for rows that do not carry one. It never uses
\verb{INSERT OR REPLACE} (rowid reassignment breaks FTS5). \code{dry_run = TRUE}
(default) validates against the column contract without touching the
database.
}
\examples{
con <- entropia_connect(system.file("extdata", "entropia-example.sqlite",
  package = "entropiaR"))
err <- tryCatch(entropia_insert(con, "items", data.frame(id = "x")), error = identity)
class(err)          # "entropia_error_write_disabled"
conditionMessage(err)
entropia_disconnect(con)
}
