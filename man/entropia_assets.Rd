% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tables.R
\name{entropia_assets}
\alias{entropia_assets}
\title{Assets (lazy)}
\usage{
entropia_assets(con)
}
\arguments{
\item{con}{A connection returned by \code{\link[=entropia_connect]{entropia_connect()}}.}
}
\value{
A \code{tbl_sql} on \code{assets}.
}
\description{
A lazy \code{\link[dplyr:tbl]{dplyr::tbl()}} over the \code{assets} table. When the schema has them
(migration 0024) the PDF page columns \code{parent_asset_id} and \code{page_number}
are exposed so pages can be joined back to their parent PDF; on older
databases the columns are simply absent. No BLOB column is ever selected:
\code{assets} carries none, and the accessor never pulls one even if a future
schema adds it.
}
\examples{
con <- entropia_connect(system.file("extdata", "entropia-example.sqlite",
  package = "entropiaR"))
entropia_assets(con)
entropia_collect(entropia_assets(con))
entropia_disconnect(con)
}
