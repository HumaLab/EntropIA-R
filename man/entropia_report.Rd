% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{entropia_report}
\alias{entropia_report}
\title{Render a frozen, shareable EDA dashboard with Quarto}
\usage{
entropia_report(x, path, redact = TRUE)
}
\arguments{
\item{x}{The complete list returned by \code{\link[=entropia_overview]{entropia_overview()}}.}

\item{path}{Destination HTML file in an existing directory.}

\item{redact}{Whether to remove paths and replace identifying labels.}
}
\value{
Normalized output path, invisibly, after successful rendering.
}
\description{
Renders the tables returned by \code{\link[=entropia_overview]{entropia_overview()}} as a standalone HTML
dashboard using the bundled Quarto template. It does not query SQLite,
recompute metrics, start a Shiny server or include document text. Quarto must
be installed separately and available on \code{PATH}; \code{ggplot2}, \code{knitr} and
\code{rmarkdown} are optional rendering dependencies. Existing destination files
are never overwritten. Temporary rendering files are removed on exit.
}
\details{
By default source paths and collection filters are removed, and collection,
entity and topic labels are replaced with report-local labels. Aggregated
values can still disclose information: review every report before sharing.
The redacted report is a presentation artifact, not a complete replay recipe.
Use \code{redact = FALSE} only for a trusted audience.
}
\examples{
\dontrun{
con <- entropia_connect("study-snapshot.sqlite")
eda <- entropia_overview(con)
entropia_disconnect(con)
entropia_report(eda, "study.html")
}
}
