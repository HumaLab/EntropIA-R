% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write.R
\name{entropia_delete}
\alias{entropia_delete}
\title{Delete rows (v2 contract; errors in v1)}
\usage{
entropia_delete(con, table, filter, all = FALSE, confirm = FALSE)
}
\arguments{
\item{con}{An EntropIA connection.}

\item{table}{Table name.}

\item{filter}{A predicate identifying the rows to delete (required in v2; a
full-table delete needs \code{all = TRUE}).}

\item{all}{Logical. In v2, permit a full-table delete when \code{TRUE} (default
\code{FALSE}).}

\item{confirm}{Logical. In v2, must be \code{TRUE} for a delete to run.}
}
\value{
Never returns: aborts with \code{entropia_error_write_disabled}.
}
\description{
The write API is designed for v2 and shipped in v1 as stubs that error with
class \code{entropia_error_write_disabled}. Every call aborts immediately with
guidance: v1 is read-only, and write support arrives in v2 via
\code{entropia_connect(path, write = TRUE)}.
}
\details{
In v2, \code{entropia_delete()} deletes rows matched by \code{filter} inside a
transaction. A full-table delete is refused unless \code{all = TRUE}, and every
delete requires \code{confirm = TRUE}.
}
\examples{
con <- entropia_connect(system.file("extdata", "entropia-example.sqlite",
  package = "entropiaR"))
err <- tryCatch(entropia_delete(con, "items", id == "x", confirm = TRUE),
  error = identity)
class(err)          # "entropia_error_write_disabled"
conditionMessage(err)
entropia_disconnect(con)
}
