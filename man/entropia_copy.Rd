% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connect.R
\name{entropia_copy}
\alias{entropia_copy}
\title{Snapshot a database to a new file (WAL-aware)}
\usage{
entropia_copy(con, dest)
}
\arguments{
\item{con}{An \code{entropia_conn} (or any DBI connection).}

\item{dest}{Destination file path. Must not exist.}
}
\value{
The normalized \code{dest} path, invisibly.
}
\description{
Copies \code{con} to \code{dest} using SQLite's \verb{VACUUM INTO}, which produces a single
consistent, self-contained database file. Because the snapshot is taken by
SQLite itself it reads through any live \code{-wal}/\code{-shm} sidecars, so it is safe
to run while EntropIA is holding the WAL. The destination must not already
exist.
}
\examples{
con <- entropia_connect(system.file("extdata", "entropia-example.sqlite",
  package = "entropiaR"))
dest <- tempfile(fileext = ".sqlite")
entropia_copy(con, dest)
copy_con <- entropia_connect(dest)
entropia_status(copy_con)
entropia_disconnect(copy_con)
entropia_disconnect(con)
}
