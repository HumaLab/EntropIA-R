% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{entropia_status}
\alias{entropia_status}
\title{Compact status summary of an EntropIA database}
\usage{
entropia_status(con)
}
\arguments{
\item{con}{A connection returned by \code{\link[=entropia_connect]{entropia_connect()}}.}
}
\value{
A \code{entropia_status} list with elements \code{path}, \code{mode},
\code{schema_version}, \code{row_counts}, \code{sync}, \code{wal} and \code{valid}.
}
\description{
Read-only snapshot of a connection: path, mode, schema version, row counts
for every readable table (descending), sync freshness from \code{sync_meta}, and
WAL state (journal mode + sidecar files). Unlike \code{\link[=entropia_validate]{entropia_validate()}},
\code{status()} never reports findings -- it describes the database.
}
\examples{
con <- entropia_connect(system.file("extdata", "entropia-example.sqlite",
  package = "entropiaR"))
entropia_status(con)
entropia_disconnect(con)
}
