% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tables.R
\name{entropia_item_topics}
\alias{entropia_item_topics}
\title{Item-topic links (lazy)}
\usage{
entropia_item_topics(con)
}
\arguments{
\item{con}{A connection returned by \code{\link[=entropia_connect]{entropia_connect()}}.}
}
\value{
A \code{tbl_sql} on \code{item_topics}.
}
\description{
A lazy \code{\link[dplyr:tbl]{dplyr::tbl()}} over the \code{item_topics} join table, linking items to
topics (one row per \verb{(item_id, topic_id)} pair, UNIQUE in the app schema).
}
\examples{
con <- entropia_connect(system.file("extdata", "entropia-example.sqlite",
  package = "entropiaR"))
entropia_collect(entropia_item_topics(con))
entropia_disconnect(con)
}
