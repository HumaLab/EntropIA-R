% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tables.R
\name{entropia_chunks}
\alias{entropia_chunks}
\title{RAG chunks (lazy)}
\usage{
entropia_chunks(con, with_vector = FALSE)
}
\arguments{
\item{con}{A connection returned by \code{\link[=entropia_connect]{entropia_connect()}}.}

\item{with_vector}{Include the \code{embedding} BLOB column. Default \code{FALSE}.}
}
\value{
A \code{tbl_sql} on \code{rag_chunks}.
}
\description{
A lazy \code{\link[dplyr:tbl]{dplyr::tbl()}} over the \code{rag_chunks} table: chunked text with
embeddings for retrieval. The chunking contract is exposed as columns
(\code{chunking_contract}, \code{embedding_model}, \code{embedding_contract}, \code{dimensions}).
The \code{embedding} BLOB is omitted by default; pass \code{with_vector = TRUE} to
select it.
}
