% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quality.R
\name{entropia_metadata_coverage}
\alias{entropia_metadata_coverage}
\title{Metadata coverage (lazy)}
\usage{
entropia_metadata_coverage(con, by = "collection")
}
\arguments{
\item{con}{A connection returned by \code{\link[=entropia_connect]{entropia_connect()}}.}

\item{by}{Grouping: \code{"collection"} (default) or \code{"item"}.}
}
\value{
A \code{tbl_sql}.
}
\description{
Reports which items have no metadata (or an empty one), per grouping.
}
\details{
With \code{by = "collection"} (default) each row is one collection with
\code{n_items}, \code{n_with_metadata}, \code{n_without_metadata} and \code{coverage}. With
\code{by = "item"} each row is one item with its collection context and a
\code{has_metadata} flag (0/1 integer). The result is lazy (\code{tbl_sql}).
}
\examples{
con <- entropia_connect(system.file("extdata", "entropia-example.sqlite",
  package = "entropiaR"
))
entropia_collect(entropia_metadata_coverage(con))
entropia_disconnect(con)
}
