% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/research.R
\name{entropia_search}
\alias{entropia_search}
\title{Search the full-text index}
\usage{
entropia_search(con, query, index = c("items", "chunks"), limit = NULL)
}
\arguments{
\item{con}{A connection returned by \code{\link[=entropia_connect]{entropia_connect()}}.}

\item{query}{A single non-empty search string.}

\item{index}{Which FTS5 index to search: \code{"items"} (default) or \code{"chunks"}.}

\item{limit}{Maximum number of results. \code{NULL} (the default) returns all
matches.}
}
\value{
A \code{tbl_sql}. The \code{items} index returns item rows (via the
\code{fts_items.rowid = items.rowid} join) plus a \code{rank} column; the \code{chunks}
index returns \code{rag_chunks} rows plus \code{rank}.
}
\description{
Parameter-safe FTS5 search over the \code{items} or \code{chunks} index. The query is
escaped with \code{\link[DBI:dbQuoteString]{DBI::dbQuoteString()}} before splicing into \code{MATCH}, so user
input can never break out of the string literal (injection-safe). Results
are returned as a lazy \code{\link[dplyr:tbl]{dplyr::tbl()}} ordered by BM25 rank (best first);
nothing is fetched until you collect.
}
\details{
The \code{items} index is the contentless \code{fts_items} table, so the join to
\code{items} on \code{rowid} is mandatory: searching without it would read \code{NULL} in
every declared column. The \code{chunks} index joins \code{rag_chunks_fts.chunk_id} to
\code{rag_chunks.id}; the \code{embedding} BLOB is never selected (BLOB discipline).
}
\examples{
con <- entropia_connect(system.file("extdata", "entropia-example.sqlite",
  package = "entropiaR"
))
entropia_collect(entropia_search(con, "huelga"))
entropia_collect(entropia_search(con, "huelga", index = "chunks", limit = 5))
entropia_disconnect(con)
}
