% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{entropia_compare_collections}
\alias{entropia_compare_collections}
\title{Compare collections (per-collection summary)}
\usage{
entropia_compare_collections(x, by = "collection_name")
}
\arguments{
\item{x}{A data frame or tibble with a collection column.}

\item{by}{The collection column, selected by name or bare (tidyselect).
Defaults to \code{collection_id} when available, otherwise \code{collection_name}.
The default ID grouping retains \code{collection_name} as a display label.}
}
\value{
A tibble with the \code{by} column, \code{n_items}/\code{n_assets} (when \code{x} carries
those id columns) and \code{n}, ordered by the collection column.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
Summarises a collected tibble one row per collection. The natural input is
the collected corpus (\code{dplyr::collect(entropia_corpus(con))}), which carries
\code{collection_name}, \code{item_id} and \code{asset_id}; pass \code{by} to group by a
different collection column (e.g. \code{collection_id}).

Each row reports \code{n} (rows of \code{x} in that collection) plus \code{n_items} and
\code{n_assets} (distinct \code{item_id} / \code{asset_id} values, \code{NA} excluded) when the
input carries those columns. Corpus rows without assets count towards \code{n}
but not \code{n_assets}. Rows are ordered by the collection column
(deterministic).
}
\examples{
con <- entropia_connect(system.file("extdata", "entropia-example.sqlite",
  package = "entropiaR"
))
corpus <- entropia_collect(entropia_corpus(con))
entropia_compare_collections(corpus)
entropia_disconnect(con)
}
