% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tables.R
\name{entropia_layouts}
\alias{entropia_layouts}
\title{Layouts (lazy)}
\usage{
entropia_layouts(con)
}
\arguments{
\item{con}{A connection returned by \code{\link[=entropia_connect]{entropia_connect()}}.}
}
\value{
A \code{tbl_sql} on \code{layouts}.
}
\description{
A lazy \code{\link[dplyr:tbl]{dplyr::tbl()}} over the \code{layouts} table. Each row is the page-layout
analysis of one asset; \code{id} is deterministic (\code{lay-{asset_id}}) and
\code{asset_id} is UNIQUE (1:1 with \code{\link[=entropia_assets]{entropia_assets()}}). \code{regions} and \code{blocks}
are JSON-in-TEXT columns, parsed by \code{\link[=entropia_collect]{entropia_collect()}}.
}
\examples{
con <- entropia_connect(system.file("extdata", "entropia-example.sqlite",
  package = "entropiaR"
))
entropia_collect(entropia_layouts(con))
entropia_disconnect(con)
}
