% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tables.R
\name{entropia_entities}
\alias{entropia_entities}
\title{Entities (lazy)}
\usage{
entropia_entities(con, include_deleted = FALSE, min_confidence = NULL)
}
\arguments{
\item{con}{A connection returned by \code{\link[=entropia_connect]{entropia_connect()}}.}

\item{include_deleted}{Include soft-deleted entities (\code{source = "manual_deleted"}). Default \code{FALSE}.}

\item{min_confidence}{Optional numeric threshold in \verb{[0, 1]}; rows with
\code{confidence < min_confidence} are excluded. \code{NULL} (default) keeps all
confidence levels.}
}
\value{
A \code{tbl_sql} on \code{entities}.
}
\description{
A lazy \code{\link[dplyr:tbl]{dplyr::tbl()}} over the \code{entities} table (named-entity recognition
output). Two filters are applied, both pushed down to SQL:
}
\details{
\itemize{
\item By default rows marked soft-deleted (\code{source = "manual_deleted"}, the
app's hidden marker) are excluded; pass \code{include_deleted = TRUE} to keep
them. On schemas that predate the \code{source} column (migration 0009) there
is no marker to honour and the filter is a no-op.
\item Set \code{min_confidence} to keep only entities at or above a confidence
threshold.
}

\code{created_at} uses the magnitude-guarded \code{datetime_auto} contract (the app
writes epoch milliseconds, the DDL default is seconds); \code{asset_id} is NULL
for item-level entities.
}
\examples{
con <- entropia_connect(system.file("extdata", "entropia-example.sqlite",
  package = "entropiaR"))
entropia_collect(entropia_entities(con))        # soft-deleted rows excluded
entropia_collect(entropia_entities(con, include_deleted = TRUE))
entropia_collect(entropia_entities(con, min_confidence = 0.9))
entropia_disconnect(con)
}
