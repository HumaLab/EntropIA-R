% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tables.R
\name{entropia_items}
\alias{entropia_items}
\title{Items (lazy)}
\usage{
entropia_items(con)
}
\arguments{
\item{con}{A connection returned by \code{\link[=entropia_connect]{entropia_connect()}}.}
}
\value{
A \code{tbl_sql} on \code{items}.
}
\description{
A lazy \code{\link[dplyr:tbl]{dplyr::tbl()}} over the \code{items} table. The \code{search_text} generated
column is included (it is cheap -- stored, not computed on read). JSON in
\code{metadata} stays raw text until \code{entropia_collect()} applies the column
contract.
}
\examples{
con <- entropia_connect(system.file("extdata", "entropia-example.sqlite",
  package = "entropiaR"))
entropia_items(con)
entropia_collect(entropia_items(con)) # metadata -> list-column, created_at -> POSIXct
entropia_disconnect(con)
}
