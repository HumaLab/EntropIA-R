% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quality.R
\name{entropia_ocr_coverage}
\alias{entropia_ocr_coverage}
\title{OCR coverage (lazy)}
\usage{
entropia_ocr_coverage(con, by = "collection")
}
\arguments{
\item{con}{A connection returned by \code{\link[=entropia_connect]{entropia_connect()}}.}

\item{by}{Grouping: \code{"collection"} (default), \code{"item"}, or \code{"asset"}.}
}
\value{
A \code{tbl_sql}.
}
\description{
Reports which assets are missing an OCR extraction, or whose extraction text
is empty/whitespace-only, per grouping. Everything stays lazy: the result is
a \code{tbl_sql} whose joins and aggregations run in SQLite when collected.
}
\details{
With \code{by = "asset"} (per-asset detail) each row is one asset with its
item/collection context and two flags:
\itemize{
\item \code{has_extraction}: the asset has an extraction row.
\item \code{text_empty}: the extraction text is empty/whitespace-only (\code{NA} when the
asset has no extraction).
}

The flags are 0/1 integers (SQLite booleans); filter with \verb{== 1}/\verb{== 0}.
With \code{by = "item"} or \code{by = "collection"} the result is a grouped summary
with \code{n_assets}, \code{n_with_extraction}, \code{n_missing} (no extraction row),
\code{n_empty} (extraction present but empty) and \code{coverage} (fraction of assets
with a non-empty extraction).
}
\examples{
con <- entropia_connect(system.file("extdata", "entropia-example.sqlite",
  package = "entropiaR"))
entropia_collect(entropia_ocr_coverage(con))
entropia_collect(entropia_ocr_coverage(con, by = "asset"))
entropia_disconnect(con)
}
