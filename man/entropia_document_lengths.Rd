% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{entropia_document_lengths}
\alias{entropia_document_lengths}
\title{Document lengths (chars/words per row)}
\usage{
entropia_document_lengths(x, text_var = "text")
}
\arguments{
\item{x}{A data frame or tibble of collected rows.}

\item{text_var}{Column holding the document text, selected by name or bare
(tidyselect). Default \code{"text"}.}
}
\value{
\code{x} with two appended columns: \code{n_chars} (characters) and \code{n_words}
(whitespace-separated tokens).
}
\description{
Appends character and word counts per row of a collected tibble. Each row
is treated as one document. \code{NA} text yields \code{NA} counts; empty or
whitespace-only text yields 0 chars and 0 words. Words are
whitespace-separated tokens (multiple spaces collapse; punctuation stays
attached to its token).
}
