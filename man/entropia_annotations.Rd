% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tables.R
\name{entropia_annotations}
\alias{entropia_annotations}
\title{Annotations (lazy)}
\usage{
entropia_annotations(con)
}
\arguments{
\item{con}{A connection returned by \code{\link[=entropia_connect]{entropia_connect()}}.}
}
\value{
A \code{tbl_sql} on \code{annotations}.
}
\description{
A lazy \code{\link[dplyr:tbl]{dplyr::tbl()}} over the \code{annotations} table (drawing/OCR-cleanup
marks on a PDF page). \code{kind} is an enum (\code{rectangle}, \code{underline}, \code{crop},
\code{erase}, \code{rotation}); \code{page} is 1-based and \code{x}/\code{y}/\code{width}/\code{height} are
coordinates in page units.
}
\examples{
con <- entropia_connect(system.file("extdata", "entropia-example.sqlite",
  package = "entropiaR"))
entropia_collect(entropia_annotations(con))
entropia_disconnect(con)
}
