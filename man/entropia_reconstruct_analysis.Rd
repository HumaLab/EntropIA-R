% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/research.R
\name{entropia_reconstruct_analysis}
\alias{entropia_reconstruct_analysis}
\title{Reconstruct LLM analyses against their targets}
\usage{
entropia_reconstruct_analysis(con, target = NULL, job_type = NULL)
}
\arguments{
\item{con}{A connection returned by \code{\link[=entropia_connect]{entropia_connect()}}.}

\item{target}{Optional single target id (an asset/item/collection id) to
keep. \code{NULL} (the default) keeps all targets.}

\item{job_type}{Optional character vector of job types to keep. \code{NULL} (the
default) keeps all.}
}
\value{
A tibble of class \code{entropia_reconstruction}.
}
\description{
Joins \code{llm_results} rows back to the row they analysed. The target table is
named per row by \code{target_type} (\code{asset}, \code{item} or \code{collection}; \code{unknown}
and missing targets have no resolvable row), so this cannot be one SQL join:
each row's target is looked up in the table its type names and returned as a
\code{target} list-column (a one-row tibble, or \code{NULL} when unresolvable). The
\code{result} JSON is parsed into a list-column and timestamps become \code{POSIXct}.
}
\details{
Materialised (one row per \code{llm_results} row) and ordered deterministically
by \code{id}; the result carries the \code{entropia_reconstruction} class.
}
