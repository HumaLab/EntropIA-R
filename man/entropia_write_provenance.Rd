% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{entropia_write_provenance}
\alias{entropia_write_provenance}
\title{Write a provenance sidecar (JSON)}
\usage{
entropia_write_provenance(x, path)
}
\arguments{
\item{x}{An object carrying a provenance stamp (see \code{\link[=entropia_provenance]{entropia_provenance()}}).}

\item{path}{Destination file path. Must be a single path.}
}
\value{
The normalized \code{path}, invisibly.
}
\description{
Persists the provenance stamp of a reproducible dataset as a JSON file, the
sidecar that travels with the analysis output. \code{NULL} values are written as
JSON \code{null} and \code{NA} values as \code{null}, so the file round-trips through
\code{\link[jsonlite:fromJSON]{jsonlite::fromJSON()}} back to the same structure (apart from the list
class). The file is UTF-8.
}
\examples{
con <- entropia_connect(system.file("extdata", "entropia-example.sqlite",
  package = "entropiaR"))
ds <- entropia_analysis_dataset(con, asset_type == "image")
path <- tempfile(fileext = ".json")
entropia_write_provenance(ds, path)
readLines(path)
entropia_disconnect(con)
}
