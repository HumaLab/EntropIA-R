% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{entropia_temporal_profile}
\alias{entropia_temporal_profile}
\title{Temporal profile (counts by time bucket)}
\usage{
entropia_temporal_profile(x, date_var, unit = "month", by = NULL)
}
\arguments{
\item{x}{A data frame or tibble of collected rows.}

\item{date_var}{A column of \code{POSIXct} (or \code{Date}) timestamps, selected by
name or bare (tidyselect).}

\item{unit}{The time bucket: \code{"second"}, \code{"minute"}, \code{"hour"}, \code{"day"},
\code{"week"} (Monday-start), \code{"month"}, \code{"quarter"} or \code{"year"}. Default
\code{"month"}.}

\item{by}{Optional column(s) to break the counts by, selected by name or
bare (tidyselect). \code{NULL} (default) produces a single time series.}
}
\value{
A tibble with the bucket column, the \code{by} columns (when given), and
a count column \code{n}.
}
\description{
Counts the rows of a collected tibble by a date/time column, bucketed to a
chosen time unit. This is an R-side analysis helper: pass a materialised
tibble (e.g. the output of \code{\link[=entropia_collect]{entropia_collect()}}, where timestamps are
\code{POSIXct}). \code{NA} timestamps are excluded -- a row with an unknown date
cannot be placed in time; grouping columns from \code{by} keep \code{NA} as their own
group, following \code{dplyr} semantics.
}
\details{
The returned bucket column carries the name of \code{date_var} and holds the
start of each unit (\code{POSIXct}, e.g. midnight for \code{unit = "day"}). Rows are
ordered by the \code{by} columns then the bucket, deterministically. Weeks start
on Monday.
}
