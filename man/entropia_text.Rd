% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/corpus.R
\name{entropia_text}
\alias{entropia_text}
\title{Per-asset best text (lazy)}
\usage{
entropia_text(con, assets = NULL, source = "auto", strip_markers = TRUE)
}
\arguments{
\item{con}{A connection returned by \code{\link[=entropia_connect]{entropia_connect()}}.}

\item{assets}{\code{NULL} for all assets, a character vector of asset ids to
keep, or a lazy table of assets (must have an \code{id} column). Default
\code{NULL}.}

\item{source}{Which text layer to use: \code{"auto"} (default), \code{"extraction"},
or \code{"transcription"}.}

\item{strip_markers}{Remove OCR page markers (\verb{![](page=n,bbox=[...])}) from
the text. Default \code{TRUE}.}
}
\value{
A \code{tbl_sql} with the input asset columns plus \code{text}.
}
\description{
Returns one row per asset with a \code{text} column holding the asset's best
available text layer:
}
\details{
\itemize{
\item \code{source = "extraction"}: the OCR extraction text
(\code{extractions.text_content}).
\item \code{source = "transcription"}: the audio transcription text
(\code{transcriptions.text_content}).
\item \code{source = "auto"} (default): the extraction text when the asset has one,
otherwise the transcription text -- the same rule the app uses to build
its search index.
}

The selection is assembled in SQL (a single \code{COALESCE} expression over the
1:1 extractions/transcriptions joins), so nothing is fetched until you
collect and there are no per-row R loops. Assets with neither layer get \code{NA}
text; they are still returned (left joins, one row per asset). The returned
columns are the input asset columns plus \code{text}.

Extraction text may embed OCR page markers of the form \verb{![](page=n,bbox=[...])}.
With \code{strip_markers = TRUE} (default) they are removed in SQL via a
recursive query, so the result stays lazy and composable with
\code{\link[dplyr:filter]{dplyr::filter()}} / \code{\link[dplyr:select]{dplyr::select()}} / friends. The marker itself is
removed exactly; surrounding whitespace is preserved.
}
\examples{
con <- entropia_connect(system.file("extdata", "entropia-example.sqlite",
  package = "entropiaR"
))
entropia_text(con)
entropia_text(con, source = "extraction", strip_markers = TRUE)
entropia_disconnect(con)
}
