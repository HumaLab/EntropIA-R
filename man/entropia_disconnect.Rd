% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connect.R
\name{entropia_disconnect}
\alias{entropia_disconnect}
\title{Close an EntropIA connection}
\usage{
entropia_disconnect(con)
}
\arguments{
\item{con}{An \code{entropia_conn} (or any DBI connection).}
}
\value{
\code{con}, invisibly.
}
\description{
Closes a connection opened by \code{\link[=entropia_connect]{entropia_connect()}}. Idempotent: calling it
again on an already-closed connection is a no-op.
}
\examples{
con <- entropia_connect(system.file("extdata", "entropia-example.sqlite",
  package = "entropiaR"
))
entropia_disconnect(con)
entropia_disconnect(con) # idempotent: safe on a closed connection
}
