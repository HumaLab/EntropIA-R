% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tables.R
\name{entropia_extractions}
\alias{entropia_extractions}
\title{Extractions (lazy)}
\usage{
entropia_extractions(con)
}
\arguments{
\item{con}{A connection returned by \code{\link[=entropia_connect]{entropia_connect()}}.}
}
\value{
A \code{tbl_sql} on \code{extractions}.
}
\description{
A lazy \code{\link[dplyr:tbl]{dplyr::tbl()}} over the \code{extractions} table. Each row is the OCR text
layer of one asset; \code{id} is deterministic (\code{ext-{asset_id}}) and \code{asset_id}
is UNIQUE, so the table joins 1:1 to \code{\link[=entropia_assets]{entropia_assets()}}. \code{text_content}
may embed PDF page markers (\verb{![](page=n,bbox=...)}) which the text layer
(\code{entropia_text()}) can strip.
}
\examples{
con <- entropia_connect(system.file("extdata", "entropia-example.sqlite",
  package = "entropiaR"
))
entropia_collect(entropia_extractions(con))
entropia_disconnect(con)
}
