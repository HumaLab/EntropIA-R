% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tables.R
\name{entropia_transcriptions}
\alias{entropia_transcriptions}
\title{Transcriptions (lazy)}
\usage{
entropia_transcriptions(con)
}
\arguments{
\item{con}{A connection returned by \code{\link[=entropia_connect]{entropia_connect()}}.}
}
\value{
A \code{tbl_sql} on \code{transcriptions}.
}
\description{
A lazy \code{\link[dplyr:tbl]{dplyr::tbl()}} over the \code{transcriptions} table. Each row is the
audio transcription of one asset; \code{id} is deterministic
(\code{trx-{asset_id}}) and \code{asset_id} is UNIQUE (1:1 with
\code{\link[=entropia_assets]{entropia_assets()}}). The \code{segments} column is JSON-in-TEXT (array of
\verb{\{start_ms, end_ms, text\}}); it stays raw until \code{\link[=entropia_collect]{entropia_collect()}}
applies the column contract.
}
\examples{
con <- entropia_connect(system.file("extdata", "entropia-example.sqlite",
  package = "entropiaR"))
entropia_collect(entropia_transcriptions(con)) # segments -> list-column
entropia_disconnect(con)
}
