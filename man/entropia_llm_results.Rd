% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tables.R
\name{entropia_llm_results}
\alias{entropia_llm_results}
\title{LLM results (lazy)}
\usage{
entropia_llm_results(con, target_type = NULL, job_type = NULL)
}
\arguments{
\item{con}{A connection returned by \code{\link[=entropia_connect]{entropia_connect()}}.}

\item{target_type}{Optional character vector of target types to keep, or
\code{NULL} (default) for all.}

\item{job_type}{Optional character vector of job types to keep, or \code{NULL}
(default) for all.}
}
\value{
A \code{tbl_sql} on \code{llm_results}.
}
\description{
A lazy \code{\link[dplyr:tbl]{dplyr::tbl()}} over the \code{llm_results} table: rows produced by LLM
jobs (summaries, analyses, ...) linked to their target via \code{target_id} plus
\code{target_type} (\code{asset}, \code{item}, \code{collection} or \code{unknown}). The \code{id} is
deterministic: \code{llr-{target_type}-{target_id}-{job_type}}. The \code{result}
column is JSON-in-TEXT, parsed by \code{\link[=entropia_collect]{entropia_collect()}} into a list-column.
}
\details{
Two optional filters, both pushed down to SQL:
\itemize{
\item \code{target_type}: keep only rows whose target is one of the given types. On
databases that predate migration 0019 the column does not exist and
passing a filter errors with guidance; without a filter the accessor
still reads the table.
\item \code{job_type}: keep only rows for one or more job types.
}
}
\examples{
con <- entropia_connect(system.file("extdata", "entropia-example.sqlite",
  package = "entropiaR"
))
entropia_collect(entropia_llm_results(con))
entropia_collect(entropia_llm_results(con, target_type = "item"))
entropia_disconnect(con)
}
