% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/corpus.R
\name{entropia_corpus}
\alias{entropia_corpus}
\title{Corpus (lazy)}
\usage{
entropia_corpus(
  con,
  collections = NULL,
  asset_types = NULL,
  text = "auto",
  page_assets = TRUE,
  include_deleted = FALSE
)
}
\arguments{
\item{con}{A connection returned by \code{\link[=entropia_connect]{entropia_connect()}}.}

\item{collections}{Optional character vector of collection names to keep
(matches \code{collections.name}). \code{NULL} (default) keeps all collections.}

\item{asset_types}{Optional character vector of asset types to keep
(matches \code{assets.type}, e.g. \code{image}, \code{pdf}, \code{audio}). \code{NULL} (default)
keeps all.}

\item{text}{The text layer to append: \code{"auto"} (default), \code{"extraction"},
\code{"transcription"}, or \code{FALSE} to omit the \code{text} column.}

\item{page_assets}{When \code{TRUE} (default) every asset is kept; when \code{FALSE}
PDF page assets (rows with a \code{parent_asset_id}) are excluded. A no-op on
schemas without the page columns (migration 0024).}

\item{include_deleted}{Reserved. The corpus tables carry no soft-delete
marker in the reference schema, so the flag currently has no effect; it is
validated and kept for API symmetry with \code{\link[=entropia_entities]{entropia_entities()}} and for
forward compatibility with schemas that introduce one.}
}
\value{
A \code{tbl_sql} with one row per asset and prefixed, non-colliding
columns from \code{items}, \code{collections} and \code{assets}, plus \code{text} unless
\code{text = FALSE}.
}
\description{
The workhorse research query: a lazy join of \code{items}, \code{collections} and
\code{assets}, one row per asset, with an optional per-asset \code{text} column.
Nothing is fetched at access time; every join and filter is pushed down to
SQLite, and the result stays composable with \code{\link[dplyr:filter]{dplyr::filter()}},
\code{\link[dplyr:select]{dplyr::select()}} and friends.
}
\details{
Column names are unambiguous across the three joined tables (e.g. \code{item_id},
\code{asset_id}, \code{collection_name}, \code{item_created_at}, \code{asset_created_at}), so
there are no name collisions. \code{metadata} is the raw JSON text of
\code{items.metadata}; use \code{\link[=entropia_metadata]{entropia_metadata()}} for the parsed form. Because the
query spans several tables, collecting it applies no column contract --
timestamps stay raw integers and \code{metadata} stays text (see
\code{\link[=entropia_collect]{entropia_collect()}}).

The \code{text} column is the asset's best text layer per \code{source}: \code{"auto"}
(default) uses the extraction text when present, otherwise the
transcription -- the app's FTS rule, assembled as one SQL \code{COALESCE}
expression. Markers are NOT stripped (use \code{\link[=entropia_text]{entropia_text()}} for stripped
text). Pass \code{text = FALSE} to omit the text layer entirely.
}
