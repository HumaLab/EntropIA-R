% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema.R
\name{entropia_schema_info}
\alias{entropia_schema_info}
\title{Tables, columns and typed contract of an EntropIA database}
\usage{
entropia_schema_info(con)
}
\arguments{
\item{con}{A connection returned by \code{\link[=entropia_connect]{entropia_connect()}}.}
}
\value{
A tibble with columns \code{table}, \code{column}, \code{type}, \code{required},
\code{contract}, \code{min_version}, \code{source}, \code{presence} (logical) and \code{live_type}.
Absent manifest tables and columns have \code{presence = FALSE} and missing
\code{live_type}; \code{type} retains the manifest declaration where available.
}
\description{
Merges the live schema (from \verb{PRAGMA table_xinfo}) with the shipped column
contract (\code{inst/schemas/manifest.json}). Every readable table gets one row
per column. \code{source} records whether the row came from the manifest contract
(\code{"manifest"}) or only exists in the live database (\code{"schema"}); columns
present in both are reported under the manifest (the contract is the typed
surface). \code{min_version} is the migration that guarantees the column, and
\code{required} marks columns whose absence is a compatibility failure once the
database reaches that version.
}
\examples{
con <- entropia_connect(system.file("extdata", "entropia-example.sqlite",
  package = "entropiaR"
))
entropia_schema_info(con)
entropia_disconnect(con)
}
