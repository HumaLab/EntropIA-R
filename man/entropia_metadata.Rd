% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/corpus.R
\name{entropia_metadata}
\alias{entropia_metadata}
\title{Item metadata (parsed)}
\usage{
entropia_metadata(con, items = NULL, parse = TRUE)
}
\arguments{
\item{con}{A connection returned by \code{\link[=entropia_connect]{entropia_connect()}}.}

\item{items}{\code{NULL} for all items, a character vector of item ids to keep,
or a lazy table of items (must have \code{id} and \code{metadata} columns). Default
\code{NULL}.}

\item{parse}{When \code{TRUE} (default) parse \code{metadata} into the tidy field
columns; when \code{FALSE} return the raw \code{metadata} text column.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per item.
}
\description{
Reads \code{items.metadata} -- JSON-in-TEXT on every item -- into tidy rows: one
row per item (in the order returned by the base query) with \code{item_id}, the
parsed \verb{__entropia_file_metadata} fields \code{original_name}, \code{original_path}
and \code{imported_at} (ISO-8601, as \code{POSIXct} in UTC), and one list-column per
remaining top-level metadata key. Items without metadata get one row with
\code{NA} in the parsed fields and \code{NULL} in the list-columns.
}
\details{
Unlike the lazy accessors this function materialises: parsing JSON to
list-columns is an R-side step. \code{parse = FALSE} returns the raw
\code{metadata} text alongside \code{item_id} instead.
}
\examples{
con <- entropia_connect(system.file("extdata", "entropia-example.sqlite",
  package = "entropiaR"))
entropia_metadata(con)
entropia_metadata(con, parse = FALSE) # raw metadata text
entropia_disconnect(con)
}
