% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{entropia_export}
\alias{entropia_export}
\title{Export data to a file}
\usage{
entropia_export(
  x,
  path,
  format = c("csv", "tsv", "json", "rds", "parquet", "arrow"),
  chunk_size = 1000L
)
}
\arguments{
\item{x}{A data frame/tibble or a lazy \code{tbl_sql} table (e.g. from
\code{\link[=entropia_corpus]{entropia_corpus()}} or \code{\link[=entropia_items]{entropia_items()}}).}

\item{path}{Destination file path (a single path; parent directories are not
created).}

\item{format}{One of \code{"csv"}, \code{"tsv"}, \code{"json"}, \code{"rds"}, \code{"parquet"} or
\code{"arrow"}.}

\item{chunk_size}{Rows fetched per chunk when streaming a lazy input to a
delimited format. Default \code{1000L}.}
}
\value{
The normalized \code{path}, invisibly.
}
\description{
Writes a data frame/tibble (or a lazy query) to a file in one of six
formats. \code{csv}, \code{tsv}, \code{json} and \code{rds} always work; \code{parquet} and \code{arrow}
(Feather v2) require the optional \code{arrow} package. A lazy \code{tbl_sql} input is
exported without being collected into memory for the delimited formats: the
query is streamed in bounded chunks via \code{DBI::dbSendQuery} +
\code{DBI::dbFetch}, so exporting a large corpus to CSV stays flat in memory.
\code{json}, \code{rds}, \code{parquet} and \code{arrow} are whole-file formats and collect the
query first.
}
\details{
Lazy queries are exported in a deterministic row order: when the rendered
SQL carries no \verb{ORDER BY}, the export arranges by the first column before
streaming. Queries with an explicit ordering (e.g. \code{\link[=entropia_search]{entropia_search()}}'s
rank order, or \code{dplyr::arrange()} applied first) are exported in that order.
Materialised data is written in the order it was given.

The column contract is NOT applied by the export: a lazy query exports the
raw values SQLite stores (epoch timestamps as integers, JSON-in-TEXT as
text). Collect with \code{\link[=entropia_collect]{entropia_collect()}} first -- and pass the result as a
tibble -- to export typed values (\code{POSIXct} timestamps and JSON list-columns).
}
\examples{
tmp <- tempfile(fileext = ".csv")
entropia_export(tibble::tibble(id = 1:2, label = c("a", "b")), tmp)
read.csv(tmp)
}
