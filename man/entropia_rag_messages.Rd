% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tables.R
\name{entropia_rag_messages}
\alias{entropia_rag_messages}
\title{RAG messages (lazy)}
\usage{
entropia_rag_messages(con)
}
\arguments{
\item{con}{A connection returned by \code{\link[=entropia_connect]{entropia_connect()}}.}
}
\value{
A \code{tbl_sql} on \code{rag_messages}.
}
\description{
A lazy \code{\link[dplyr:tbl]{dplyr::tbl()}} over the \code{rag_messages} table: the ordered messages of
a conversation. \code{role} is \code{user} or \code{assistant}; \code{sort_index} gives the
order within a conversation. The \code{sources} column is JSON-in-TEXT (an array
of \verb{\{chunk_id, text, score\}} citations on assistant messages), parsed by
\code{\link[=entropia_collect]{entropia_collect()}} into a list-column.
}
\examples{
con <- entropia_connect(system.file("extdata", "entropia-example.sqlite",
  package = "entropiaR"))
entropia_collect(entropia_rag_messages(con)) # sources -> list-column
entropia_disconnect(con)
}
