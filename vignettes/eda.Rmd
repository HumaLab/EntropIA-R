---
title: "Análisis exploratorio de un corpus EntropIA"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Análisis exploratorio de un corpus EntropIA}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r setup, include = FALSE}
knitr::opts_chunk$set(collapse = TRUE, comment = "#>", fig.width = 6, fig.height = 4)
library(entropiaR)
library(dplyr)
```

*Versión en español.* English: `vignette("eda.en")`.

Empezá con un snapshot y un solo overview. No bajes el corpus completo hasta
que los recuentos confirmen el universo.

```{r}
con <- entropia_connect(system.file(
  "extdata", "entropia-example.sqlite",
  package = "entropiaR"
))
```

# Inventario sin cargar texto

`entropia_overview()` agrega en SQLite. Nunca materializa OCR, JSON ni BLOB.

```{r}
eda <- entropia_overview(con)
names(eda)
eda$counts
eda$collections
eda$asset_types
```

`counts` declara unidad (`item`, `asset`, `collection`). El `n` de
`collections` es filas de la consulta de estudio (assets más ítems sin asset),
no un sinónimo de `n_items`.

# Un universo, muchas tablas

```{r}
pdfs <- entropia_overview(con, asset_types = "pdf", page_assets = FALSE)
pdfs$counts
pdfs$selection
```

Un vector de IDs vacío no selecciona “todo”: selecciona nada.

```{r}
entropia_overview(con, collection_ids = character())$counts
```

# Calidad: numerador, denominador, status

```{r}
eda$quality
```

Leé `status` antes que `pct`:

- `ok` — la tasa está definida;
- `empty` — hay filas elegibles pero el campo/capa está vacío;
- `no_data` — denominador 0;
- `not_applicable` — la métrica no aplica a ese grupo;
- `invalid` — JSON que no parsea.

`metadata_coverage` es *presencia* de texto. `metadata_validity` es sintaxis
JSON, no “el documento tiene autor y fecha”.

# Entidades y temas: ocurrencia vs prevalencia

```{r}
eda$entities
eda$topics
```

`n` son menciones (filas). `n_items` son ítems distintos. `pct` es
`n_items / total` con `total` = ítems del universo — los ítems sin entidad
siguen en el denominador.

# Tiempo y exclusiones

```{r}
eda$temporal
attr(eda$temporal, "exclusions")
```

Los buckets son inicios de mes UTC de `date_var` (defecto `item_created_at`).
Esa fecha es **operativa** (alta/importación), no necesariamente la del
documento histórico.

# Perfil de un tibble recolectado

```{r}
lengths <- entropia_text(con) |>
  entropia_collect() |>
  entropia_document_lengths()
entropia_profile(lengths, columns = c("n_chars", "n_words"))
```

El muestreo es opt-in y no cambia `.Random.seed`:

```{r}
set.seed(1)
before <- .Random.seed
entropia_profile(lengths, sample_n = 2, seed = 99)$sampling$n
identical(.Random.seed, before)
```

El mismo objeto `eda` alimenta ggplot, el dashboard y `entropia_report()`.

```{r}
entropia_disconnect(con)
```

Siguiente: `vignette("visualize")`. English: `vignette("eda.en")`.
